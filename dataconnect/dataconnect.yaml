specVersion: "v1beta"
serviceId: "omnitriage"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "omnitriage-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
